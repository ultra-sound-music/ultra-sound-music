import Avatar, { IAvatarProps } from './Avatar';
import { ComponentStory, ComponentMeta } from '@storybook/react';
declare const _default: ComponentMeta<
  ({ image, imageUrl, size, shape }: IAvatarProps) => JSX.Element
>;
export default _default;
export declare const Template: ComponentStory<typeof Avatar>;
export declare const imageUrl: ComponentStory<
  ({ image, imageUrl, size, shape }: IAvatarProps) => JSX.Element
>;
export declare const ImageComponet: ComponentStory<
  ({ image, imageUrl, size, shape }: IAvatarProps) => JSX.Element
>;
