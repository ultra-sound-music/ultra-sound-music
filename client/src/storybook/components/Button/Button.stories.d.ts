import { ComponentStory, ComponentMeta } from '@storybook/react';
declare const _default: ComponentMeta<
  ({
    isPrimary,
    style,
    size,
    text,
    subText,
    image,
    to,
    onClick,
    ...props
  }: import('./Button').IButtonProps) => JSX.Element
>;
export default _default;
export declare const Primary: ComponentStory<
  ({
    isPrimary,
    style,
    size,
    text,
    subText,
    image,
    to,
    onClick,
    ...props
  }: import('./Button').IButtonProps) => JSX.Element
>;
export declare const Secondary: ComponentStory<
  ({
    isPrimary,
    style,
    size,
    text,
    subText,
    image,
    to,
    onClick,
    ...props
  }: import('./Button').IButtonProps) => JSX.Element
>;
export declare const Large: ComponentStory<
  ({
    isPrimary,
    style,
    size,
    text,
    subText,
    image,
    to,
    onClick,
    ...props
  }: import('./Button').IButtonProps) => JSX.Element
>;
export declare const Small: ComponentStory<
  ({
    isPrimary,
    style,
    size,
    text,
    subText,
    image,
    to,
    onClick,
    ...props
  }: import('./Button').IButtonProps) => JSX.Element
>;
export declare const Light: ComponentStory<
  ({
    isPrimary,
    style,
    size,
    text,
    subText,
    image,
    to,
    onClick,
    ...props
  }: import('./Button').IButtonProps) => JSX.Element
>;
export declare const Dark: ComponentStory<
  ({
    isPrimary,
    style,
    size,
    text,
    subText,
    image,
    to,
    onClick,
    ...props
  }: import('./Button').IButtonProps) => JSX.Element
>;
